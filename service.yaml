apiVersion: v1
kind: Service
metadata:
  name: {{ include "MEDIA-WIKI.fullname" . }}
  labels:
    {{- include "MEDIA-WIKI.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "MEDIA-WIKI.selectorLabels" . | nindent 4 }}
